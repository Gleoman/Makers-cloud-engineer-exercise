<h1> Today's FT_Ratings </h1>


  <% if flash[:notice] %>
    <div class="notice"><%= flash[:notice] %></div>
  <% end %>

  <div class="review wrapper">
    <% @review.each do |r| %>
      <div class="rating">
        <%= r.rating %>
      </div>
    <% end %>
  </div>

<%= link_to 'New', new_review_path %>
